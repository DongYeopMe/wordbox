spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wordtree
    username: root
    password: thrmEkel123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  session:
    store-type: redis
    timeout: 30m
  redis:
    host: localhost
    port: 6379
